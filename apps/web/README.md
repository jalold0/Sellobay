# @ecom/web — Mijoz sayti (Next.js)

Next.js 14 App Router asosida mijoz sayti.

- App Router (`src/app/[locale]/...`)
- next-intl (uz / ru / en)
- TanStack Query
- Tailwind + shadcn/ui (`@ecom/ui` orqali)

## Boshlash

```bash
pnpm dev          # http://localhost:3000
pnpm build && pnpm start
```
