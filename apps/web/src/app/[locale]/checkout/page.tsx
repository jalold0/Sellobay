import { CheckoutFlow } from '../../../components/checkout/checkout-flow';

export const metadata = { title: 'Buyurtmani rasmiylashtirish' };

export default function CheckoutPage() {
  return <CheckoutFlow />;
}
