# @ecom/mobile — Mijoz mobil ilovasi (Expo)

React Native (Expo SDK 51, Expo Router) asosida iOS/Android ilova.

```bash
pnpm dev          # Expo Dev Server
pnpm android      # Android emulator
pnpm ios          # iOS simulator
```
